<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="{{ ctx.sitemap_ns }}">
{% for url in ctx.sitemap_urls %}
  <url>
    <loc>{{ url.file_path | as_full_url }}</loc>
    <lastmod>{{ url.last_modified.date().isoformat() }}</lastmod>
  </url>
{% endfor %}
</urlset>
